# 日报 — 2026-05-15（Session 49）

## 概述
发布前最后一轮 bug 修复，收尾 Session 47 聊天逻辑和 Session 45 遗留问题。

## 修复清单

### 学习页标题
- `'拾词集 · 学习中'` → `'学习中'`（study_page.dart:179）

### 每日一词切单词本不再变化
- **根因**：`currentWordGone` 用 `existsByTextInNotebook` 只查当前单词本，切本后旧词不在新本里被误判"已删除"
- **修复**：改用 `existsByText` 跨全部单词本查
- **规则**：只在每日更新、点换一个、词被删除、数据清空时变

### 聊天逻辑（Session 47 收尾）
- copyWith null bug：`newSession()`/`switchSession()` 直接用构造函数
- 每次打开 = 新对话，旧的在历史记录
- 会话惰性创建，不再有空会话残留
- AI 标题 = 第一条消息原文

### 红屏修复
- capture_sheet `reset()` 移入 Future.microtask
- _confirm() 防重复点击

### 导入修复
- 文件读取 bytes fallback（Android 分区存储）
- 恢复离线词典富化（enrichWithLlm: false）
- _pickFile() try-catch

### 小修复
- API 配置下拉框 ValueKey 强制重建
- 隐私文案更新

## APK
`build/dist/WordSnap-v1.0.0-release-20260515.apk`

## 待定
- 无，准备发布
