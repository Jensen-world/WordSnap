import 'package:flutter/material.dart';

class AppRadius {
  static const BorderRadius pill = BorderRadius.all(Radius.circular(100));
  static const BorderRadius card = BorderRadius.all(Radius.circular(20));
  static const BorderRadius input = BorderRadius.all(Radius.circular(12));
  static const BorderRadius sheet = BorderRadius.vertical(top: Radius.circular(20));
}
