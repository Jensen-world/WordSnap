import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color signalBlue = Color(0xFF2F5CFF);
  static const Color amberFlash = Color(0xFFFFA940);

  // Neutrals
  static const Color inkBlack = Color(0xFF0B0B0F);
  static const Color canvasWhite = Color(0xFFF9F9FB);

  // Semantic
  static const Color mint = Color(0xFF00C896);
  static const Color lavender = Color(0xFF7068F0);

  // Borders & surfaces
  static const Color border = Color(0xFFE2E2EA);
  static const Color cardFill = Color(0xFFFFFFFF);
  static const Color subtleFill = Color(0xFFF0F0F5);
  static const Color hintText = Color(0xFFBBBBBB);
}
